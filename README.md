# Meteora Sniper Engine v3.0\n\nProduction Solana sniper for new Meteora pools. Helius WS → Jupiter V6 → Jito bundler.\n\n## Deploy\n1. Fork this repo\n2. Railway → New Project → Deploy from GitHub\n3. Add env vars from `.env.example`\n4. Deploy!\n\n## Supabase Schema\n```sql\nCREATE TABLE pools (pool_address TEXT PRIMARY KEY, token_b TEXT, risk_score INT, status TEXT, created_at TIMESTAMPTZ);\nCREATE TABLE trades (id TEXT PRIMARY KEY, pool_address TEXT, signature TEXT, status TEXT, created_at TIMESTAMPTZ);\nCREATE TABLE whitelist (pool_address TEXT PRIMARY KEY);\nALTER TABLE pools, trades ENABLE REPLICA;\n```